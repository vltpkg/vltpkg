# @vltpkg/vlt-darwin-arm64

This is the macOS ARM64 (Apple Silicon) binary for
[`vlt`](https://www.npmjs.com/package/vlt).

This package is installed automatically by `vlt` on the appropriate
platform. You should not need to install it directly.
