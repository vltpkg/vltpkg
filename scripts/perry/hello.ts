// Phase 0 gate probe. Keep trivial: proves toolchain compiles + links + runs.
// Not the future vlt entry point (that lands in Phase 3f).
function main(): void {
  console.log('hello from perry')
}

main()
