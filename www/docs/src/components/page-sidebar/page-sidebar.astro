---
import { PageSidebar as ReactPageSidebar } from './page-sidebar.tsx'

const { toc } = Astro.locals.starlightRoute
---

{
  toc && (
    <ReactPageSidebar {...Astro.locals.starlightRoute} client:load />
  )
}
